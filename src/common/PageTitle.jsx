
const PageTitle = ({title}) => {
  return (
    <div className="text-center w-full text-[25px] sm:text-[30px] md:text-[40px] lg:text-[50px] font-bold text-white">{title}</div>
  )
}

export default PageTitle